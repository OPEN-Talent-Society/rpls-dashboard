---
description: "Render requested creative asset"
allowed-tools: ["HTTP", "Bash", "Read"]
---

# /creative:render

Invoke `video-generate` or `image-variant` skill based on asset type and return storage link + metadata.
